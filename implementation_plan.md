# 一键排版双模式切换与进度日志展现 - 实施计划

根据您的需求，现有的“一键执行并弹窗”的简单逻辑已经不满足要求，我们需要一个**可视化的排版控制台**。排版必须做到“过程透明”且“灵活可控”。

## User Review Required

> [!IMPORTANT]
> 这是一个较大的 UI/UX 和架构调整。为了满足进度展示和分步执行，我计划引入一个全新的“主控制面板 (MainPanelControl)”替换原来一点击 Ribbon 就执行底层的逻辑。请确认以下设计是否符合您的预期。

## 界面与交互设计规划

当您点击 Ribbon 上的“一键排版”时，不再直接在后台静默执行，而是会在右侧弹出一个**“排版控制台”侧边栏**。

侧边栏包含两个核心区域：

1. **操作区 (支持双模式切换)**：
   - **模式 1：全自动巡航模式**：只有一个“开始”按钮。点击后，按照标准的顺序执行（清洗 -> 样式 -> 解析排版 -> 图表 -> 目录），同时底部的进度条会实时滚动。
   - **模式 2：分步精准打击模式**：展示五个独立操作按钮：
     1. `[清理脏数据]`
     2. `[重置标准样式]`
     3. `[解析并排版正文]` (执行此步时，如果发现大纲问题，依然可以唤醒大纲确认树)
     4. `[排版图表]`
     5. `[生成目录]`
     这五个按钮相互独立，您可以不按顺序随意点击执行。
2. **日志输出区 (Console Log)**：
   - 在侧边栏的最下方，放置一个黑色背景的黑客风（或简洁风）日志文本框。
   - 无论是模式 1 还是模式 2，在执行底层代码时，都会实时打印：“[清洗模块] 发现连续空行，正在清除...”、“[样式模块] 正在覆盖正文基准字体为宋体...”、“[图表模块] 发现 5 张图片，已全部居中”。

## Proposed Changes (代码架构变更)

### 1. 新增排版控制台 UI (UI)
- **[NEW]** `UI/MainPanelControl.cs`：核心面板控件。包含选项卡（或单选框）切换模式 1 和 2，以及包含日志 `TextBox` 和 `ProgressBar`。
- **[MODIFY]** `UI/TaskPaneManager.cs`：增加管理并展示 `MainPanelControl` 的功能。

### 2. 抽离排版工作流总线 (Workflow)
为了让 UI 能随时调用单独的步骤，并且能随时接收日志，我们需要把 `RibbonAssistant` 里的硬编码抽离出来。
- **[NEW]** `Workflows/FormatWorkflow.cs`：
  提供独立的 API，如 `ExecuteClean()`, `ExecuteStyleRebuild()`, `ExecuteMediaFormat()`。
  定义事件 `Action<string> LogMessage`，允许底层模块把日志发给 UI。

### 3. 底层日志接入 (Cleaners, Formatters, etc.)
- **[MODIFY]** `Cleaners/DocumentCleaner.cs`、`Styles/StyleManager.cs` 等模块：
  为这些类增加日志回调函数，让它们在干活时能往外“喊话”。例如：记录删了多少个空行，改了多少个表。

### 4. Ribbon 入口修改 (Integration)
- **[MODIFY]** `RibbonAssistant.cs`：点击“一键排版”直接调用 `TaskPaneManager.ShowMainPanel()`。

## 验证计划

1. 编译通过后，点击 Ribbon“一键排版”，检查侧边栏是否正常弹出双模式。
2. 测试分步模式：只点击“清理脏数据”，观察日志框是否输出正确，文档空行是否消失。
3. 测试全自动模式：观察进度条和日志是否连贯输出。

---
请点击“批准 (Approve)”，我将为您编写这套全新的可视化工作流。
